// AOS Script start
AOS.init({
  duration: 900,
});
// AOS Script end
// GSAP Script start
gsap.registerPlugin(TextPlugin);
gsap.to('.descTestimonial', { duration: 2.9, delay: 3, text: '" Bekerja dengan suasana hati yang lebih asik, menyenangkan dan mempelajari hal baru setiap harinya. "' });
// GSAP Script end
